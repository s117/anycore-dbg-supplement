sret:
  if no exception frame is met until stack is poped to empty:
    interpolate a incomplete function record warning (issue to recorder)
    interpolate a exception record (issue to recorder)
ret:
  if stack is empty:
    interpolate a function record (issue to recorder)

finalize:
  if no poweron reset:
    interpolate an incomplete stack warning
